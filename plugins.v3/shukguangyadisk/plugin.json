{
  "name": "光鸭云盘助手",
  "description": "MoviePilot V3 光鸭云盘存储助手：自动整理按资源文件夹建立私有任务，常规目录由 MoviePilot 一次规划整包内容，任务完成前不会被保存/扫描动作拆成新的逐文件批次。",
  "version": "3.4.2",
  "icon": "Guangyadisk_A.png",
  "author": "liheng-lk",
  "level": 1,
  "system_version": ">=3.0.0",
  "history": {
    "v3.4.2": "按实机日志修复自动整理任务粒度：此前 folder_stream 只在扫描/历史层按子目录分组，真正提交私有 worker 时仍逐文件调用 TransferChain，导致 MoviePilot 反复出现‘正在计划整理 1 个文件’，一个资源目录被拆成多个独立整理批次；现在监控根直接子目录作为一个私有 folder task，常规 SxxExx/电影目录直接调用 TransferChain.do_transfer(directory, background=False)，由 MoviePilot 一次递归规划整个目录并在同一任务内逐文件整理。22~[4K] 等依赖插件上下文的弱命名保留在同一 folder task 内逐文件兼容执行。成员文件继续使用原有 history gate、inflight/retry/blocked/completed 状态与最终事件回写。保存同一监控目录设置不再把 last_tick 清零强制开启下一轮扫描，避免当前目录尚未整理完就被保存操作切成新批次。",
    "v3.4.1": "修复 v3.4.0 私有整理队列完成一批后可能等待完整扫描周期才继续的问题：正常新资源发现仍遵守用户配置的 interval，但插件私有队列降至低水位且上轮存在 capacity_wait 时，会由 10 秒轻量心跳提前补充下一批；首轮仍处于稳定性等待且私有队列为空时也会短周期复查。无 backlog 时不会高频访问光鸭 API。连续补充仍使用同一子目录流式状态机、MoviePilot history gate 与 TransferChain.do_transfer(background=False)，不接触 MoviePilot 全局后台整理队列。",
    "v3.4.0": "自动整理执行架构彻底隔离：光鸭扫描按监控根直接子目录流式处理，候选文件只进入插件私有 Queue/独立 Worker，再通过 TransferChain.do_transfer(background=False, manual=False) 同步调用 MoviePilot 原生整理业务链；不再进入 MoviePilot 全局 background queue，也不再写该队列的 pending 回放登记，因此本地硬盘、下载器和其它原生自动整理不会再被光鸭批量任务堵塞。目标目录、媒体识别、重命名、移动/复制、覆盖、刮削和整理历史仍由 MoviePilot 决定。升级时只读检查旧光鸭全局队列，存在旧 waiting/running 时拒绝新增并提示重启 MoviePilot 一次；旧任务排空后自动恢复光鸭自动监控。新增跨热重载单 owner 防护，长同步任务未结束时延迟 teardown，禁止新旧插件实例并行执行；私有 worker 重启会把未终态任务安全迁回 retry。新增按子目录分组的持久历史，且候选过滤不再实例化 TransferDispatcher。",
    "v3.3.2": "紧急队列恢复：确认 v3.3.0/3.3.1 自动整理会把大量光鸭文件提交到 MoviePilot 全局后台整理队列，而该队列同时承载本地硬盘和其它原生整理；大量远程任务会导致全局队列 0% 等待、原生自动整理也无法消费。此版立即暂停光鸭自动提交，只隔离当前监控目录下、存储类型为光鸭的新旧名称的待整理回放登记，并对当前进程中的对应路径设置协作取消；绝不清理本地/其它存储任务、不直接修改 MP 私有队列、不重启 worker。升级后重启 MoviePilot 一次即可丢弃旧内存光鸭队列且不再回放，账号、存储、浏览、上传和手动整理保持可用；被隔离文件保留为 retry 状态，等待 v3.4 独立调度器恢复。",
    "v3.3.1": "紧急兼容修复：对齐 MoviePilot V3 当前 snapshot_storage(storage, path, last_snapshot_time, max_depth, previous_snapshot) 契约，修复 previous_snapshot 关键字导致光鸭存储快照直接抛 TypeError；快照按 MP 完整基线语义保留未变化目录、清理已移走文件并补齐 fileid。将 MoviePilot history gate 改为惰性可选能力，宿主未暴露内部 history helper 时自动交回 TransferDispatcher 原生门控，不再阻断插件安装；字幕/音频等可选事件改为按宿主能力注册，避免 V3 小版本事件枚举差异造成导入失败。",
    "v3.3.0": "专业化重构自动整理架构：拆分扫描编排、持久状态机、MoviePilot 历史预检、媒体识别上下文和运行时事件桥；新增 stabilizing/inflight/retry/blocked/completed/ignored 六态模型，只有 MP 最终成功事件或有效成功历史才能标记完成，异常和未接收入队均可恢复重试；复用 MoviePilot 自身 history gate，失败预算耗尽进入可诊断门控并支持定时/手动重新检查；完善热重载生命周期、自检与状态控制台。识别侧修复发布目录污染标题，针对“【站点】偏爱靠近你[短剧][全31集].../Close.to.You.S01E27...”优先提取干净本地化标题并保留年份、季集提示，避免整段发布名进入 TMDB 识别。",
    "v3.2.2": "修复自动整理只显示“已提交 MP”但后台不真正整理的问题：为当前 MoviePilot V3 补齐 StorageOperSelection 运行时桥，确保整理链能够取得光鸭源存储操作对象；新增 MP 最终 TransferComplete/TransferFailed 回执，失败会重新开放源文件自动重试，不再把“进入队列”误当成整理完成。识别侧扩展为文件名 + 父目录 + Season/Sxx + TV/电影根目录 + MoviePilot 目录配置的高置信度联合判断，S01E43、1x43、第N集/话、Season 目录等明确按电视剧进入 MP，避免剧集被识别成电影。升级后一次性重新开放 v3.2.1 已标记 seen 的文件，已成功项仍由 MoviePilot 整理历史去重，旧版提交后失败项会重新尝试。",
    "v3.2.1": "修复数字集号弱文件名识别：当文件名类似 22~[4K][HEVC.AAC]、23~[...] 时，使用父目录剧名 + 数字集号作为 MoviePilot 原生识别提示；分类、目标目录、重命名、整理方式、覆盖、刮削和历史仍全部由 MoviePilot 原生整理链负责。",
    "v3.2.0": "重构网盘整理为自动目录监控：只配置光鸭监控目录、扫描周期、稳定等待与批次上限；新增设置持久化、立即扫描、运行状态和监控记录。插件不再自建分类/目标目录/移动复制/覆盖规则，新增或变化文件统一送入 MoviePilot 原生 TransferDispatcher/TransferChain，由 MP 当前目录配置负责媒体识别、分类、重命名、目标路径、整理方式、覆盖、刮削、历史去重与失败重试。",
    "v3.1.0": "新增网盘整理：直接读取 MoviePilot 媒体库目录配置作为分类策略，按 MP 媒体识别结果、媒体类型/类别、类型/类别子目录规则生成光鸭云盘内整理预览；支持自动按优先级匹配或指定策略、移动/复制、覆盖策略保护、目录浏览、预览后二次确认执行、配套字幕跟随与整理历史。",
    "v3.0.0": "MoviePilot V3 专用首版：采用 V3 稳定 SDK、明确 API 响应模型、V3 Federation API 注入壳和可重复生命周期清理；保留双登录、存储扩展、整理上传、142 目录恢复、145/147 落盘确认、上传幂等、进度监控与 WebDAV。"
  }
}
